# Workflow pilot kit

This synthetic workflow draft is an importer-compatible, reviewable description
of a small request-intake path. It does not implement or claim a workflow runtime.

Import `project.json` through the FoundRy UI, inspect the dependency sequence and
exception path, then export it for review. Every acceptance case remains `not-run`.
Record an observed result only after a real bounded review.
